# IDE Extension Non-Goals

- Feature commits on `main` without PR from `impl/atlas-extension`
- Camera / microphone integration (camera = 0)
- Silent auto-commit of secrets
- Making the extension the only memory store (Git `state/` is canonical)
- Tracking `node_modules/` or `*.vsix` in Git
- Full AI Runtime / Plugin Host / Knowledge services (separate issues)
