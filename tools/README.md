# Tools Layer

Inventory for Atlas CLI utilities. **Min-scope list:** `INVENTORY.md`.

## Hierarchy (build last)

1. Existing features → 2. Add-ons → 3. OSS → 4. Lightweight scripts → … → new programs last.

## Quick smoke (M6)

```bash
bash tools/atlas_status.sh
```

Primary agent work surface remains **Cline** (D15), not custom IDE extension (D22).
