# atlas-vscode-extension

Local Ollama-backed Atlas assistant sidebar for VS Code.

- Spec / checklist: `docs/04_IDE_EXTENSION/`
- Track: [issue #2](https://github.com/ln9swrd/atlas/issues/2)
- Branch for features: `impl/atlas-extension`

```bash
npm install
npm run compile
# F5 in VS Code — see docs/04_IDE_EXTENSION/F5_CHECKLIST.md
```

Do not commit `node_modules` or `*.vsix`. See `docs/04_IDE_EXTENSION/PACKAGING.md`.
