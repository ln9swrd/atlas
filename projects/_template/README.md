# Project template

1. Copy `state/` to `projects/<name>/state/`
2. Replace `<project-name>` and placeholders
3. Follow `docs/process/PROJECT_STATE_SCHEMA.md`

Do not use this for Atlas DevOS itself (root `state/` is separate).
Project SERA is deprecated (D19) — do not create `projects/sera`.
