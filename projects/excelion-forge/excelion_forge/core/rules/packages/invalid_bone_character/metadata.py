"""Package metadata for the invalid bone character rule."""

from __future__ import annotations

from ...metadata import RuleCategory
from ...metadata import RuleMetadata


METADATA = RuleMetadata(
    rule_id="EF102",
    category=RuleCategory.BONE,
)
