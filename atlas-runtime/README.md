# atlas-runtime (experimental)

Platform P3 kernel stubs. **Not** daily-ops SoR (`tools/` is).

| Item | Note |
|------|------|
| Dir name | `atlas-runtime` (hyphen) — not a normal import package name |
| Smoke | `python tools/check_atlas_runtime.py` (repo root) |
| Status | Stub pipeline only; no product coupling |

Do not auto-load in agent context unless P3 work.
