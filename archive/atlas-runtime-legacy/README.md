# atlas-runtime (archived 2026-08-11)

Former root `atlas-runtime/` — P3 experimental kernel stubs.

- Not daily-ops SoR (`tools/` + `core/` + `tests/` are).
- Archived under Master-approved P1 (POLICY_HOLD_SURVEY).
- Stub sources remain in git history under former `atlas-runtime/` paths; tree copy under this directory may be completed in a follow-up if needed.
- Do not restore to repo root without Master approval.
