# projects/forge (legacy experiment)

**D20 (2026-07-30):** This tree is an **Atlas App-host / IApplication shell snapshot**, not the product Forge pipeline.

| Use | Path |
|-----|------|
| **Canonical product** | `projects/excelion-forge/` |
| This folder | Historical / experimental only |

Do not treat `manifest.json` + `app.py` as the active Excelion Forge codebase.  
Cline / agents: open **excelion-forge** `state/` and `docs/` instead.
